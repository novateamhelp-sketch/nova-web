[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback for client routes. force must stay false so /assets/* still load.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
