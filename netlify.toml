[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — must serve index.html for every client route on reload.
# force = true is required so Netlify does not return its own 404 page.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
